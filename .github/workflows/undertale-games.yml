name: Undertale game collection
on:
  push:
    paths:
      - '.github/undertale-games.py'
      - '.github/repair-ui.py'
      - '.github/stable-runtime-hotfix.py'
      - '.github/site-finalizer.py'
      - 'undertale-thumbnails/**'
  schedule:
    - cron: '29 2 * * *'
  workflow_dispatch:
permissions:
  contents: write
concurrency:
  group: ubg43-maintenance
  cancel-in-progress: false
jobs:
  undertale:
    if: ${{ !contains(github.event.head_commit.message, '[undertale-builder]') || github.event_name != 'push' }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Add and maintain Undertale games
        run: python3 .github/undertale-games.py
      - name: Remove duplicate game cards
        run: python3 .github/dedupe-games.py
      - name: Rebuild stable UBG43 homepage
        run: python3 .github/repair-ui.py
      - name: Harden stable runtime
        run: python3 .github/stable-runtime-hotfix.py
      - name: Apply final link and runtime sanity fixes
        run: python3 .github/site-finalizer.py
      - name: Commit Undertale collection
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add index.html legacy-index.html undertale-thumbnails
          git commit -m 'Refresh Undertale game collection [undertale-builder]' || exit 0
          git push
